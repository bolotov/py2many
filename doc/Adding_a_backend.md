
**Do not** attempt to add a backend (yet).

- API is unstable
- Project is yet to be stabilized

## How adding a backend may have looked like

- Project woud need a protocol which a backend (target)
 would need follow to work.

- A process of adding a backend must look like filling
  gaps in what needs to be specified and implmented
  including specifying all transformers/rewriters
  and whatever else (to be defined)

- Optional fields in a protocol would be a nice thing
  like MIME type (or **maybe not** because what for?)
